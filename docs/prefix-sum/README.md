# Prefix Sum (누적합)

[← 메인으로 돌아가기](../../README.md)

---

## 🔑 키워드
`구간 합` `부분 합` `O(1) 조회` `전처리`

## 🧩 자주 섞이는 조합
- 누적합 + 투포인터
- 누적합 + 이분 탐색
- 2D 누적합 + 구간 쿼리

## ❌ 실수 포인트
- 인덱스 off-by-one 에러
- 0번 인덱스 처리 (prefix[0] = 0)
- 2D에서 중복 빼기/더하기 실수

---

## 📊 시간 복잡도

| 연산 | 일반 | 누적합 |
|-----|-----|-------|
| 전처리 | - | O(N) |
| 구간 합 쿼리 | O(N) | O(1) |

---

## 1D 누적합

```java
// 전처리: prefix[i] = arr[0] + arr[1] + ... + arr[i-1]
int[] prefix = new int[N + 1];
for (int i = 0; i < N; i++) {
    prefix[i + 1] = prefix[i] + arr[i];
}

// 구간 합 [L, R] (0-indexed)
int sum = prefix[R + 1] - prefix[L];
```

> **핵심**: `prefix[i]`는 `arr[0]` ~ `arr[i-1]`까지의 합

---

## 2D 누적합

```java
// 전처리
int[][] prefix = new int[N + 1][M + 1];
for (int i = 0; i < N; i++) {
    for (int j = 0; j < M; j++) {
        prefix[i + 1][j + 1] = arr[i][j]
            + prefix[i][j + 1]
            + prefix[i + 1][j]
            - prefix[i][j];
    }
}

// 구간 합 (r1, c1) ~ (r2, c2) (0-indexed)
int sum = prefix[r2 + 1][c2 + 1]
        - prefix[r1][c2 + 1]
        - prefix[r2 + 1][c1]
        + prefix[r1][c1];
```

> **포함-배제 원리**: 더하고 빼서 정확한 구간만 계산
