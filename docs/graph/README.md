# 그래프 (Graph)

[← 메인으로 돌아가기](../../README.md)

---

## 📎 관련 문서
- [BFS / DFS](../bfs-dfs/README.md)
- [트리](../tree/README.md)

---

## 정의

정점(Vertex)과 간선(Edge)으로 이루어진 자료구조

---

## 용어

| 용어 | 설명 |
|-----|------|
| 정점 (Vertex) | 그래프를 구성하는 점 / 노드 |
| 간선 (Edge) | 정점과 정점을 연결하는 선 |
| 차수 (Degree) | 정점에 연결된 간선의 수 |
| 경로 | 정점을 연결된 간선을 따라 탐색하는 순서 |
| 사이클 | 시작 정점과 끝 정점이 동일한 경로 |
| 연결 요소 | 서로 도달할 수 있는 모든 정점들의 묶음 |

---

## 그래프 종류

| 종류 | 설명 |
|-----|------|
| 무방향 그래프 | 간선에 방향이 없음 (양방향) |
| 방향 그래프 | 간선에 방향이 있음 (진입/진출 차수) |
| 가중치 그래프 | 간선에 비용이 할당됨 |

---

## 그래프 표현 방법

| 방식 | 공간 | 전체 탐색 | 연결 확인 |
|-----|-----|---------|---------|
| 인접 행렬 | O(V²) | O(V²) | O(1) |
| 인접 리스트 | O(V+E) | O(V+E) | O(deg) |

> 일반적으로 **인접 리스트**가 유리 (간선이 밀집하지 않은 경우)

---

## 🧱 기본 코드

**인접 행렬**
```java
int[][] adj = new int[V + 1][V + 1];
adj[src][dst] = 1;
```

**인접 리스트**
```java
ArrayList<Integer>[] graph = new ArrayList[V + 1];
for (int i = 0; i <= V; i++) {
    graph[i] = new ArrayList<>();
}
graph[src].add(dst);
```

**가중치 그래프 (인접 리스트)**
```java
class Node {
    int node;
    int cost;

    Node(int node, int cost) {
        this.node = node;
        this.cost = cost;
    }
}

ArrayList<Node>[] graph = new ArrayList[V + 1];
```
